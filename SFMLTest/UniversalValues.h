#pragma once

#include "Vector2D.h"

//For now a just a (human) reference for certain important sizes
//THAT MUST BE USED LIKE THIS EVERYWHERE ELSE
class UniversalValues
{
public:
	//float const deltaTime = 0.0167f;
	//const Vector2D screenSize = Vector2D(1920, 1080);
};
